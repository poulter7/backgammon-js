var assert = require('assert')
describe('Game', function(){
	describe('#start', function(){
		it('should have the standard setup for a backgammon game', function(){
			[].indexOf(5).should.equal(-1);
			[].indexOf(5).should.equal(2);
		})
	})
})

